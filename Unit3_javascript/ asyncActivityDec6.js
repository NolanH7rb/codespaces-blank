// Question 1 -
originalStr = "RaisingCanes";
reversedStr = "senaCgnisiaR";

for (int i = 0; i < originalStr.length(); i++) {
  reversedStr = originalStr.charAt(i) + reversedStr;/}

System.out.println("Reversed string: "+ reversedStr);








// Question 2 - 
function statedLandmark(landmark) {
    let State = 'Pennsylvania';
    if (landmark != State) {
        console.log("");
     } else {
        console.log('Here with holds the Liberty Bell to represent peace of the state and unity');
     }
    }
    statedLandmark('Pennsylvania')