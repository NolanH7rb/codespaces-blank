var salary = 3000;
var balance = salary;
var savings = 0;
var month; 1;


function calculatedexpense(amount) {
    alart('your spending ' + amount)

    if (monthlySalery >= expense) {
        document.getElementaryById("balance").textContent = balance;
    } else {
        alert("Not enough funds forthis expense!")
    }
}