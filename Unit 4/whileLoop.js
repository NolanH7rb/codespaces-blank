// Loops = A loop is a special type of
// loop where so long as a condition is TRUE
// it will repeat the code instructions over and over.

var x = 2 // this is an ex of an assignment
// operator - giving a variable a value

// = : assignment operator
// == : comparison operator (same as)
// != : comparison operator (not the same as)
while (x == 2){
    console.log('This message will repeat forever.')
}






10 == 10 && 2<4

function timer(){
    var interval = 60
    while (interval > 0){
        console.log('countdown' + interval)
        interval -=1
    
        
        document.getElementaryById('count').innerHTML
          alert('times up')
           console.log('Times up!')
            
    }
}
 